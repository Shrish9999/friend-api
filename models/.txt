1. What is a Database?

Simple Definition:
Database ek digital “Godaam” (warehouse) ya “Register” hai jahan hum data (information) store karte hain.

Samajhne ke liye:
Jaise aapke phone ki Gallery mein photos store hoti hain,
ya Phonebook mein contacts save hote hain.

Wo peeche ek database hi use kar rahe hote hain taaki aap jab chahein photo dekh sakein ya number dial kar sakein.

2. Why do we use Databases?

Hum data ko text file ya notebook mein bhi likh sakte hain, par database kyu chahiye?

🔐 Security

Notebook kho sakti hai,
database mein password, access control aur backup hota hai.

⚡ Speed

Agar register mein 1 lakh naam hain, toh “Rahul” dhundhne mein ghante lag sakte hain.
Database milliseconds mein search kar leta hai.

👥 Multiple Users

Ek saath 10–1000 log data use kar sakte hain
(jaise IRCTC par ek time pe ticket booking).

3. Types of Databases (Relational & Non-Relational)
🔹 Relational Database (RDBMS / SQL)

Excel Sheet jaisa hota hai

Rows aur Columns hote hain

Data strict rules follow karta hai

Examples: MySQL, PostgreSQL

🔹 Non-Relational Database (NoSQL)

Folder jaisa hota hai jisme alag-alag files hoti hain

Fixed row/column structure nahi hota

Data flexible hota hai

Examples: MongoDB, Cassandra

4. Basic Concepts (SQL vs MongoDB)
Concept	SQL (Relational)	MongoDB (NoSQL)	Easy Meaning
Container	Table	Collection	Ek poora register / sheet
Data Entry	Row	Document	Ek insan ki puri detail
Data Label	Column / Field	Field	Name, Age, Email
Primary Key	Primary Key	_id	Unique Roll Number
Link	Foreign Key	Reference	Tables ko jodna
5. SQL vs NoSQL Differences
📐 Structure

SQL:

Pehle se decide karna padta hai (Name, Age only)

Naya field add karna mushkil

NoSQL:

Kabhi bhi kuch bhi add kar sakte ho

Aaj Age, kal Hobbies bhi add ho sakti hain

📈 Scaling

SQL:

Vertical Scaling (mehengi RAM / CPU lagao)

NoSQL:

Horizontal Scaling (naye saste servers add karo)

6. Schema vs Schema-less Databases
🎓 Schema (Strict Rule – SQL)

School uniform jaisa

Sabka format same

Age number hi honi chahiye, text nahi

👕 Schema-less (Flexible – MongoDB)

Casual kapdon jaisa

Kisi document mein 3 fields, kisi mein 10

Flexible structure

7. Introduction to MongoDB

MongoDB ek NoSQL database hai

JSON style data store karta hai { key: value }

BSON

MongoDB internally BSON (Binary JSON) use karta hai

JSON ka fast version

Images, videos jaisa data bhi handle karta hai

Hierarchy

Database (School)
→ Collection (Classroom)
→ Document (Student)

8. MongoDB CRUD Operations

CRUD = Create, Read, Update, Delete

➕ Create (Insert)
db.users.insertOne({ name: "Rahul", age: 25 })

🔍 Read (Find)
db.users.find({ name: "Rahul" })

✏ Update
db.users.updateOne(
  { name: "Rahul" },
  { $set: { age: 26 } }
)

❌ Delete
db.users.deleteOne({ name: "Rahul" })

9. Important Concepts in MongoDB
📚 Indexes (Book Index Page)

Index na ho toh poori book palatni padegi

Index banane se search super fast ho jati hai

🧃 Aggregation (Juicer Mixer)

Data ko filter + process karta hai

Example:
“Sirf Delhi ke users nikalo aur unki average age batao”

✅ Validation Rules

MongoDB flexible hai, par rules laga sakte ho:

Email mandatory

Age 18 se kam nahi honi chahiye

10. MongoDB Connection with Node.js

MongoDB aur Node.js ke beech hum Mongoose use karte hain.

Mongoose kya hai?

Translator + Bodyguard

MongoDB ke upar schema (rules) laga deta hai

Code clean aur safe rehta hai

Installation
npm install mongoose

Basic Connection Code
const mongoose = require("mongoose");

// MongoDB connection URL
const uri = "mongodb://localhost:27017/myDatabase";

mongoose.connect(uri)
  .then(() => console.log("Bhai, Database connect ho gaya!"))
  .catch((err) => console.log("Connection fail ho gaya yaar", err));